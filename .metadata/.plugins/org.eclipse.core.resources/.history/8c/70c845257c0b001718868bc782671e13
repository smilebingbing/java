package com.java.offer;

public class HasSubTree {

	public boolean hasSubTree(NodeTree root1, NodeTree root2){
		boolean result = false;
		while(root1 != null && root2 != null){
			if(root1.value == root2.value){
				result = hasSubTree(root1, root2);
			}
			if(!result){
				
			}
		}
	}
	
	public boolean doTree1HasTree2(NodeTree root1, NodeTree root2){
		if(root2 == null){
			return true;
		}
		if(root1 == null){
			return false;
		}
		if(root1.value != root2.value){
			return false;
		}
		return doTree1HasTree2(root1.getLeft(), root2.getLeft()) && 
				doTree1HasTree2(root1.getRight(), root2.getRight());
	}
}
